# 算法动画讲解网站规格说明

## 1. 概念与愿景

一个交互式的算法可视化学习平台，通过动画演示帮助理解核心数据结构与算法的执行过程。界面设计融合科技感与学术气质，让学习算法变得直观有趣。支持Python代码与伪代码双视图同步展示，可自定义演示数据。

## 2. 设计语言

### 美学方向
科技感学术风格 - 深色背景搭配渐变色数组条，代码区域采用等宽字体高亮显示

### 色彩方案
- 主色: `#6366f1` (靛蓝紫)
- 次色: `#22d3ee` (青色)
- 强调色: `#f472b6` (粉红，用于高亮当前操作)
- 背景: `#0f172a` (深蓝黑)
- 卡片背景: `#1e293b`
- 文字: `#e2e8f0`
- 成功: `#10b981`
- 警告: `#f59e0b`

### 字体
- 标题: "Orbitron", sans-serif (科技感)
- 正文: "Inter", system-ui
- 代码: "JetBrains Mono", "Fira Code", monospace

### 动效哲学
- 数组元素交换: 0.5s ease-in-out 滑动+缩放
- 高亮变化: 0.3s 背景色渐变
- 步骤切换: 淡入淡出 0.4s
- 进度条平滑填充

## 3. 布局与结构

### 页面结构
```
┌─────────────────────────────────────────────────────────┐
│  Header: Logo + 算法选择下拉 + 速度控制 + 主题切换      │
├─────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────┐   │
│  │           可视化画布 (数组条形图)                 │   │
│  │    █ █ █ █ █ █ █ █ █ █ █ █ █ █ █ █           │   │
│  └─────────────────────────────────────────────────┘   │
├─────────────────────────────────────────────────────────┤
│  控制条: [生成随机] [开始] [暂停] [单步] [重置]          │
├────────────────────────┬────────────────────────────────┤
│    伪代码面板          │        Python代码面板          │
│    (左侧高亮当前行)     │        (同步高亮)               │
├────────────────────────┴────────────────────────────────┤
│  步骤说明: 当前操作的文字描述                            │
├─────────────────────────────────────────────────────────┤
│  自定义输入: 输入数组数据，逗号分隔                       │
└─────────────────────────────────────────────────────────┘
```

### 响应式策略
- 桌面 (>1024px): 双栏代码面板并排
- 平板 (768-1024px): 代码面板堆叠
- 手机 (<768px): 简化控制，代码区域可折叠

## 4. 功能与交互

### 核心功能
1. **算法选择**: 下拉菜单选择不同算法
2. **可视化动画**: 数组条形图实时反映算法状态
3. **代码同步**: 伪代码和Python代码同步高亮当前执行行
4. **播放控制**: 开始/暂停/单步执行/重置
5. **速度调节**: 滑块控制动画速度 (0.25x - 4x)
6. **自定义数据**: 输入自定义数组或随机生成
7. **步骤记录**: 记录所有操作步骤，可前后浏览

### 支持的算法
- 冒泡排序 (Bubble Sort)
- 选择排序 (Selection Sort)
- 插入排序 (Insertion Sort)
- 快速排序 (Quick Sort)
- 归并排序 (Merge Sort)
- 二分搜索 (Binary Search)

### 交互细节
- 悬停数组元素: 显示数值tooltip
- 点击数组元素: 高亮选中
- 播放中禁用数据输入
- 完成后显示"完成"提示

## 5. 组件清单

### AlgorithmSelector
- 下拉选择器，悬停展开列表
- 当前选中项高亮显示

### VisualizationCanvas
- SVG/Canvas绘制数组条形
- 条形高度与数值成正比
- 交换动画: 两元素滑动到对方位置
- 比较动画: 两元素变粉红色高亮
- 有序动画: 变绿色

### CodePanel
- 语法高亮代码显示
- 当前行高亮背景
- 行号显示

### ControlBar
- 图标按钮组
- 播放状态指示
- 进度指示器

### SpeedSlider
- 滑块 + 倍速显示
- 范围 0.25x - 4x

### CustomInput
- 输入框 + 解析按钮
- 错误提示

## 6. 技术方案

- 纯HTML5 + CSS3 + Vanilla JavaScript
- 无外部依赖，单文件可运行
- 使用CSS Grid/Flexbox布局
- CSS变量管理主题
- requestAnimationFrame驱动动画
- 状态机管理算法执行流程
